# FastAPI stack
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3

# Per-IP rate limiting on /compile (slowapi wraps starlette middleware).
slowapi==0.1.9

# YAML parsing (intake_yaml field)
pyyaml==6.0.2

# In-process embeddings (avoids needing an Ollama sidecar on Fly).
# Loads nomic-embed-text-v1.5 ONNX weights once at first request.
fastembed==0.8.0

# hcc-compiler is installed editable from repo root in the Docker image;
# its transitive deps (pydantic, pyyaml) are already listed above.
